{
  "name": "covid-medical-exemptions",
  "version": "0.0.1",
  "description": "OAS (Swagger v3) API Definition for Template API",
  "scripts": {
    "lint": "openapi-generator-cli validate -i specification/covid-medical-exemptions.yaml",
    "publish": "openapi-generator-cli generate -i specification/covid-medical-exemptions.yaml --generator-key v3.0 -g openapi -o build && rm openapitools.json && mv build/openapi.json build/covid-medical-exemptions.json",
    "check-licenses": "node_modules/.bin/license-checker --failOn GPL --failOn LGPL"
  },
  "author": "NHS Digital",
  "license": "MIT",
  "homepage": "https://github.com/NHSDigital/covid-medical-exemptions",
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.1",
    "newman-reporter-htmlextra": "^1.20.1",
    "openapi-validator-middleware": "^3.2.3"
  },
  "devDependencies": {
    "apigeetool": "^0.5.4",
    "license-checker": "^25.0.1",
    "minimist": "^1.2.2",
    "newman": "^5.3.0",
    "newman-reporter-html": "^1.0.5"
  }
}
